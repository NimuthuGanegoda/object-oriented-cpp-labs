#include<iostream>

int main(){
    // The cons keyword specifies that a variable's value is constant 
    // tells the compiler to prevent anything from modifing it 
    // (read-only)
    double pi = 3.14;
    double radius = 10;
    double circumference = 2 * pi * radius;

    std::cout




    return 0;
}